# Copy this file to .env and fill in the values from Supabase (Settings -> API).
# In Vercel, add these same two as Environment Variables.
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-public-key
